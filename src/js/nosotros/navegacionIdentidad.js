import { logoActions, himnoActions, creadorActions } from './identidad.js'
import { nosotros } from './variables.js';
export const touchNavegacion = (identidadContenido, nextVirtud, backVirtud) => {
    
}